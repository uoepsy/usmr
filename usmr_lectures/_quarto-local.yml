project:
  output-dir: LOCAL
  post-render: rsync -auvh LOCAL/ ../LOCAL_SLIDES/
revealjs-plugins:
  - drop
format:
  revealjs:
    drop:
      engine: webr
      webr:
        packages:
          - dplyr
          - ggplot2
resources:
  - assets/theme_tune.mp3
